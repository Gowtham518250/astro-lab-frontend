import Container from "@/components/common/container";
import SectionTitle from "@/components/common/section-title";

const categories = [
  "Physics",
  "Chemistry",
  "Biology",
  "Astronomy",
  "Robotics",
  "Mathematics",
];

export default function Categories() {
  return (
    <section className="py-24">
      <Container>
        <SectionTitle
          title="Science Categories"
          subtitle="Choose your favorite field and start learning."
        />

        <div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
          {categories.map((category) => (
            <div
              key={category}
              className="rounded-xl border bg-white p-8 text-center shadow transition hover:-translate-y-2 hover:shadow-xl"
            >
              <h3 className="text-2xl font-bold">
                {category}
              </h3>
            </div>
          ))}
        </div>
      </Container>
    </section>
  );
}